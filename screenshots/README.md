# Screenshots

Place the demo screenshots here with these exact filenames (referenced by the
main README):

- `ask.png`      — the "Ask (single model)" tab with an answer + agent trace
- `compare.png`  — the "Compare two models" tab (qwen vs deepseek side-by-side)
- `evaluate.png` — the "Evaluate models" tab with the question set + model checkboxes

Any PNG works; these names just need to match the image links in ../README.md.
